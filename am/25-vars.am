AM_EXTRA_DIST = Makefile.in Makefile.am $(EXTRA_DIST)

#+$if _AM_IS_TOP_BUILDDIR
AM_DIST_TOP_BUILDDIR_FILES = configure configure.ac $(AC_AUX_FILES) $(AC_MACRO_FILES)
#+$endif

AM_DIST_FILES = $(AM_DIST_TOP_BUILDDIR_FILES) $(AM_AUTO_DIST_FILES) $(AM_EXTRA_DIST)
